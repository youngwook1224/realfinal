<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cafe.dao.ReservationDAO">
    <insert id="insertReservation">
        INSERT INTO reservations (user_id, reservation_date, reservation_time, number_of_people, seat_number)
        VALUES (#{userId}, #{reservationDate}, #{reservationTime}, #{numberOfPeople}, #{seatNumber})
    </insert>

    <select id="getReservationsByUserId" resultType="com.example.cafe.dto.ReservationDTO">
        SELECT * FROM reservations WHERE user_id = #{userId}
    </select>
</mapper>
